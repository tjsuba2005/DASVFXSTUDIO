{
    // Use IntelliSense to learn about possible attributes.
    // Hover to view descriptions of existing attributes.
    // For more information, visit: https://go.microsoft.com/fwlink/?linkid=830387
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Attach to Chrome",
            "port": 9222,
            "request": "attach",
            "type": "chrome",
            "webRoot": "${workspaceFolder}"
        },

        {
            "name": "Attach to Chrome",
            "port": 9222,
            "request": "attach",
            "type": "chrome",
            "webRoot": "${workspaceFolder}"
        },
        

        {
            "type": "chrome",
            "request": "launch",
            "name": "Launch Chrome against localhost",
            "url": "http://localhost:3001",
            "webRoot": "${workspaceFolder}"
        },
{
    "type": "node",
    "request": "launch",
    "name": "Launch Backend (Nodemon)", // A friendly name for the dropdown
    "runtimeExecutable": "nodemon",      // Use nodemon to run the file
    "program": "${workspaceFolder}/server.js", // The entry point of your server
    "restart": true,                     // Automatically restart on changes
    "console": "integratedTerminal",     // Show output in the VS Code terminal
    "internalConsoleOptions": "neverOpen"
}
    ]
}