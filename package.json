{
  "name": "my-app-backend",
  "version": "1.0.0",
  "description": "The backend API for my application",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "install-server": "npm install",
    "install-client": "cd client && npm install"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2"
  },
  "engines": {
    "node": "18.x"
  }
}
